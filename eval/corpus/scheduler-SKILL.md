# Scheduler Skill

## Scheduled Tasks

The scheduler can dispatch one-time, interval, and cron tasks to the agent when
it is idle. After a scheduled task is handled, the agent marks it done through
the scheduler CLI. This fixture is a related operations doc but not a recall
ranker design doc.

